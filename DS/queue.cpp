#include <iostream>
#include <queue>
#include <climits>
using namespace std;

int main()
{

    int area = INT_MIN;
    int are1 = INT_MAX;
    cout << area << endl;
    cout << are1 << endl;
    // queue<int> q;

    // q.push(17);
    // cout << "Front in queue" << q.front() << endl ;
    // q.push(10);
    // q.push(16);

    // q.pop();
    // cout << "Front in queue" << q.front() << endl ;

    // cout << "Size of queue :" << q.size() << endl ;

    // if(q.empty()) {
    //     cout << "Queue is empty " << endl ;
    // }
    // else {
    //     cout << "Queue is not empty " << endl ;
    // }

    return 0;
}
