#include<iostream>
using namespace std;

int main(){
    /*
    int i = 5 ;
    int *p = &i ;
    // int *p = 0;
    // p = &i;
    // cout << p << endl; 
    // cout << *p << endl; 

    // (*p) = (*p) + 1 ;
    p = p + 5 ;
    cout << p << endl ;
    // cout << *p << endl; 

    //copy pointers

    // int *q = p ;
    // // q = p ;
    // cout << *q << '-' << *p << endl ;
    // cout << q << '-' << p << endl ;

    // // (*p)++;
    // // *p = *p + 2 ;
    // // *p = *p * *p ;
    // // *p = *p % *p ;
    // // *p = *p*2 + *p + (*p*6)/2 + 2 ;
    // cout << *p << endl;
    // cout << p << endl;
    */

    // int arr[] = {1,2,3,4} ;
    // int *p = &arr[0] ;
    
    // p = p + 1 ;
    // cout << p << endl ;
    // cout << *p << endl;
    // cout << &p << endl ;
    
    // cout << arr << endl;
    // cout << *arr << endl ;
    // cout << *(arr+1) << endl ;
    // cout << *arr + 1 << endl ;
    // cout << &arr << endl ;
    // cout << &arr[0] << endl; 

    int arr[] = {4,5,6,7} ;
    int*p = (arr + 1) ;
    cout << *arr  << endl ;
    // cout << p  << endl;
    // cout << *p << endl;
    // cout << &p << endl;

    switch (expression)
    {
    case /* constant-expression */:
        /* code */
        break;
    
    default:
        break;
    }


    return 0;
}